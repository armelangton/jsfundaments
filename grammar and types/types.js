let number =10;
console.log(number); 

stringOne stringTwo are dataTypes used to represent text. Wrap them in single 
or double quotes 
let stringOne = "double quotes;
let stringTwo="single quotes";

console.log(stringOne, strongTwo);

// Booleans = true or false 

let on = true
let off = false
console. log (on or off)

// Null = Null is an empty value

let empty = null
console.log(empty);

// undefined= no value is assigned and does not act as an empty container
let undef=undefined;
console.log(undef);

    let correct;
    console.log (correct);

    // OBJEXTS
// Key = "Frodo" and "hobbit = value" that's why it has "" the string will always hold the ""
    let frodo ={
        race: "hobbit",
        rings: 1
        cloak: true

    }
    console.log(frodo)
    console.log(typeof frodo);

    // Arrays => container that hold list of items
    let list = [`item1`, `item2`, `item3`];
    console.log(list);

    let burritos = ['large', 4, true];
    console.log(burritos);
    consolde.log(typeof burritos);

    // DataType Literals
    // A literal represent a fixed value that we as developers insert into the code
    // Includes stringOne, numbers, booleans, objects, arrays

    //String literal
    let car ='Ford';
    //Numeric literal
    let tired = true;
    //Object literal
    let soup = {
        a: 'chicken noodle',
        b: 'tomato',
        c: 'beef and barley',
    };

    //Array Literal

    let days = ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday'];





